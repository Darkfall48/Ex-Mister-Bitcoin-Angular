import { FetchDataPipe } from './fetch-data.pipe';

describe('FetchDataPipe', () => {
  it('create an instance', () => {
    const pipe = new FetchDataPipe();
    expect(pipe).toBeTruthy();
  });
});
